create table monitorhf (
    ids__uid VARCHAR ,
    ids__interval VARCHAR,
    interval__raw VARCHAR,
    interval__start TIMESTAMP,
    interval__end TIMESTAMP,
    extra varchar,
    PRIMARY KEY (ids__interval)
);

