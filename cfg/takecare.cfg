create table takecare (
ids__uid VARCHAR ,
ids__interval VARCHAR,
interval__start TIMESTAMP,
interval__end TIMESTAMP,
extra varchar,
FOREIGN KEY (ids__uid) REFERENCES overview(ids__uid)
PRIMARY KEY (ids__uid)
);


